{
  "name": "alpine-forms",
  "version": "0.0.1",
  "description": "An AlpineJs forms Library",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "docs:dev": "npm run build && docsify serve docs",
    "prepare": "husky"
  },
  "peerDependencies": {
    "alpinejs": "^3.14.8"
  },
  "dependencies": {
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
